<?xml version="1.0" encoding="utf-8"?>
<!--
    全螢幕相機 Overlay（嵌在 GutterFormActivity 內，避免 Activity 切換造成放大/旋轉卡頓）
    - 預覽「看到即是拍到」：不裁切，必要時留黑邊（fitCenter）
    - 版型以手機直立 UI 呈現：橫放拍攝時快門維持在「實體下方」
      （順時針橫放＝按鈕在畫面右側；逆時針橫放＝按鈕在畫面左側）
    - 預覽區顯示為 3:4（寬=螢幕寬，高=4/3 寬）；相機在橫放時透過 targetRotation 讓 4:3 畫面旋轉成 3:4 對齊此框
    - orientationWarning：裝置未橫放時顯示提示遮罩並禁用快門
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:clickable="true"
    android:focusable="true">

    <!-- 預覽區（螢幕座標 3:4：寬=螢幕寬，高=4/3 寬） -->
    <FrameLayout
        android:id="@+id/previewContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#000000"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.5"
        app:layout_constraintDimensionRatio="W,3:4">

        <!-- CameraX 預覽：不裁切（fitCenter），必要時留黑邊 -->
        <androidx.camera.view.PreviewView
            android:id="@+id/viewFinder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:scaleType="fitCenter" />

    </FrameLayout>

    <!-- 側邊控制列：橫放拍攝時維持在「實體下方」（順/逆時針會落在左右側），覆蓋在預覽上 -->
    <LinearLayout
        android:id="@+id/controlsContainer"
        android:layout_width="108dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:background="#AA000000"
        android:elevation="8dp"
        android:paddingTop="24dp"
        android:paddingBottom="24dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1">

        <ImageButton
            android:id="@+id/btnCancel"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_close"
            android:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="取消" />

        <View
            android:layout_width="1dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <ImageButton
            android:id="@+id/btnCapture"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@null"
            android:background="@drawable/bg_shutter_button"
            android:contentDescription="拍照" />

        <View
            android:layout_width="1dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <View
            android:layout_width="48dp"
            android:layout_height="48dp" />

    </LinearLayout>

    <!-- 橫向提示遮罩：裝置未橫放時顯示 -->
    <FrameLayout
        android:id="@+id/orientationWarning"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#CC000000"
        android:visibility="visible"
        android:elevation="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center">

            <ImageView
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:src="@drawable/ic_screen_rotation"
                android:contentDescription="@null" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/msg_landscape_camera_title"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_marginTop="16dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/msg_landscape_camera_hint"
                android:textColor="#CCCCCC"
                android:textSize="13sp"
                android:layout_marginTop="6dp" />

        </LinearLayout>

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
