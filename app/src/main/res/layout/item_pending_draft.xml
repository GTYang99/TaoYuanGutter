<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:paddingHorizontal="20dp"
    android:paddingVertical="10dp">

    <!-- 標題：側溝草稿序號 -->
    <TextView
        android:id="@+id/tvPendingDraftTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/text_grey"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/ivPendingArrow"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 副標題1：建立時間 -->
    <TextView
        android:id="@+id/tvPendingDraftTime"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/inputFieldHint"
        android:textSize="11sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/ivPendingArrow"
        app:layout_constraintTop_toBottomOf="@id/tvPendingDraftTitle" />

    <!-- 副標題2：已存節點數量 -->
    <TextView
        android:id="@+id/tvPendingDraftNodes"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:textColor="@color/inputFieldHint"
        android:textSize="11sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/ivPendingArrow"
        app:layout_constraintTop_toBottomOf="@id/tvPendingDraftTime" />

    <!-- 箭頭指示 -->
    <ImageView
        android:id="@+id/ivPendingArrow"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_arrow_forward"
        android:contentDescription="查看草稿"
        app:tint="@color/inputFieldHint"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- 分隔線 -->
    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/border_grey"
        android:layout_marginTop="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvPendingDraftNodes" />

</androidx.constraintlayout.widget.ConstraintLayout>
