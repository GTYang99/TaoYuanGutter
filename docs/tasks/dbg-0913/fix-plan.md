# Fix Plan

1. 抽出或新增可測試的無法開蓋清除內容判斷，依實際清除欄位與照片 slot 2／3 決定是否需要既有確認 Alert。
2. 在實際離開 `GutterFormActivity` 前集中復用既有基本欄位與照片完整性檢查；未完成時顯示單鍵通知，確認後呼叫原本的保存與返回流程。
3. 保留檢視→編輯→預覽的早期返回分支。
4. 加入單元與 Android UI 測試，執行受影響測試、單元測試與 debug build。
