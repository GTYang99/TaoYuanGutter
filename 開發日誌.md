# TaoYuanGutter 開發日誌

最後整理：2026-08-20

這份文件整合了原本分散在多個 `*.md` 的修改紀錄、流程說明、架構圖、API 規格、相機設定與錯誤分類。目的不是再做一次產品說明，而是讓後續的 Codex 或 Gemini 可以快速理解：

- 目前系統的核心資料流
- 已完成的功能調整
- 重要的 API 合約
- 相機、地圖、表單與草稿之間的互動
- 目前已知的技術決策與注意事項

## 專案定位

TaoYuanGutter 是 Android Kotlin 側溝巡查應用，主要流程是：

1. 地圖上選點或檢視既有側溝
2. 進入表單填寫基本資料
3. 拍攝或補拍照片
4. 提交結構化資料與照片
5. 必要時回到地圖繼續編輯或檢視

核心資料橋接物件是 `Waypoint`。它負責把地圖座標、表單欄位、照片 URI 和 API 回傳的 `nodeId` 串在一起。

## 核心資料流

### Waypoint 是中樞

`Waypoint` 主要包含：

- `id`
- `nodeId`
- `index`
- `latLng`
- `waypointType`
- `basicData`
- `photoUris`

其中 `basicData` 是表單欄位的主要承載體，常見 key 包含：

- `NODE_TYP`
- `MAT_TYP`
- `XY_NUM`
- `IS_CANTOPEN`
- `NODE_DEP`
- `NODE_WID`
- `IS_BROKEN`
- `IS_HANGING`
- `IS_SILT`

### 標準工作流程

1. 地圖選點產生 `Waypoint`
2. `AddGutterBottomSheet` 或 `GutterFormActivity` 編輯資料
3. `GutterBasicInfoFragment` 和 `GutterPhotosFragment` 收集資料
4. `GutterRepository` 組出 API request
5. `storeDitch()` 或 `submitGutter()` 送出結構化資料
6. 照片再透過 `uploadNodeImage()` 上傳
7. 成功後把 `nodeId`、`spiNum` 或相關識別資訊回寫到本地狀態

### 無法開蓋邏輯

如果 `IS_CANTOPEN = true`，提交時會自動清除深度和寬度等量測欄位。這是目前固定規則，不是例外處理。

## 表單與模式

### 主要角色

- `MainActivity`：地圖入口、路線與線段顯示、模式切換
- `AddGutterBottomSheet`：新增與編輯路點的浮動表單
- `GutterFormActivity`：表單容器與驗證中心
- `GutterBasicInfoFragment`：基本資料頁
- `GutterPhotosFragment`：照片頁
- `GutterInspectActivity`：既有側溝的唯讀檢視
- `PendingDraftsBottomSheet`：草稿清單與恢復入口

### 模式差異

#### 新增模式

- 側溝編號欄位可見
- 必填驗證完整啟用
- 送出後建立新的 `spiNum`

#### 編輯模式

- 側溝編號欄位隱藏
- `XY_NUM` 會自動帶入
- `gutterId` 驗證會跳過
- 編輯完成後會重新整理地圖上的線段

#### 檢視模式

- 只讀
- 點選後可進一步進入編輯
- 會優先取最新的點位資料，而不是只依賴地圖上已有座標

### 草稿機制

`GutterSessionRepository` 負責暫存和恢復未送出的資料。文件中提過兩層概念：

- 內存暫存：當前編輯中的資料
- Room 持久化：用來保存待上傳草稿

舊版 SharedPreferences 資料有做遷移處理。

## API 合約

### StoreDitch

主要 API 為：

```text
POST /api/v1/ditch/storeDitch
```

資料結構重點：

- `spiNum`：新增時可為空，更新時需帶入
- `nodes`：點位列表

每個節點通常需要：

- `node_id`
- `NODE_ATT`
- `NODE_NUM`
- `NODE_TYP`
- `MAT_TYP`
- `latitude`
- `longitude`
- `NODE_LE`
- `XY_NUM`
- `IS_CANTOPEN`
- `IS_BROKEN`
- `IS_HANGING`
- `IS_SILT`
- `NODE_DEP`
- `NODE_WID`
- `NODE_NOTE`

### 節點意義

- `NODE_ATT = 1`：起點
- `NODE_ATT = 2`：節點
- `NODE_ATT = 3`：終點

`NODE_NUM` 只在中間節點使用，且通常是遞增序號。

### 典型回應

成功後會回傳：

- `spiNum`
- 每個節點的 `nodeId`
- 節點回填後的資料

失敗時會出現：

- `422` 驗證失敗
- `401` 認證失敗
- 其他伺服器或網路錯誤

### API 上傳流程

1. 先送結構化資料
2. 再逐張上傳照片
3. 照片 API 以 `node_id` 和 `fileCategory` 區分用途

## 相機與照片

### 相機架構

目前表單內主要使用 `CameraOverlayFragment`，搭配 CameraX。文件中的結論是：

- 預覽畫面和最終 JPEG 是兩件不同的事
- 預覽負責讓使用者構圖
- 最終檔案由 CameraX 依 targetRotation 與 EXIF 規則處理

### 方向邏輯

相機方向的流程大致是：

1. `OrientationEventListener` 讀取裝置角度
2. 判斷是否為橫向
3. 設定 `preview.targetRotation`
4. 設定 `imageCapture.targetRotation`
5. CameraX 寫入 EXIF Orientation
6. 上傳時保留原始 JPEG 與 EXIF

目前的實作重點是：

- 橫向時快門可用
- 直立時快門禁用
- 不重新編碼照片，以免丟失方向資訊

### 完成狀態

相機橫向拍攝相關修改已視為完成，包含：

- 預覽區比例調整為 4:3
- 方向提示與快門狀態控制
- EXIF 方向保留

## 地圖與線段

### 顯示策略

編輯、新增、檢視模式啟用時，地圖會盡量只顯示正在操作的側溝，不讓其他線段干擾判讀。模式結束後會重新恢復整體線段顯示。

### 重要控制點

- `MainActivity` 是地圖模式切換的主入口
- `ScopeMapCoordinator` 和 `ScopeGutterPolylineController` 負責 scope 視圖
- `DistanceMeasureManager` 負責測距虛線與粗度
- `InspectMarkerController` 負責檢視模式的標記

### 測距設定

測距線有動態粗度配置：

- 小距離：細線
- 中距離：標準線
- 大距離：粗線
- 超大距離：更粗線

手動也可以直接設定線寬。

## 介面調整紀錄

### RadioGroup 取代下拉選單

原本 `IS_BROKEN`、`IS_HANGING`、`IS_SILT` 這些欄位曾以 Dropdown 呈現，後來評估後改成 `RadioGroup`。

原因很單純：

- 二選一比下拉更快
- 選項少時一眼可見
- 不需要多一次展開動作

目前建議是：

- 二選項用水平排列
- 三到四選項用垂直排列

### 視覺重點

這次調整的目標不是增加功能，而是讓使用者不用猜目前選了什麼，也不用多點一次才看得到選項。

## 錯誤分類與提示

上傳失敗已經整理成可讀的分類，不直接把 raw log 或 stack trace 丟給使用者。

常見分類包含：

- 網路連線失敗
- 側溝資料上傳失敗
- 照片處理失敗
- 照片上傳失敗
- 伺服器回應逾時
- 未知錯誤

設計原則是：

- 畫面只顯示分類、說明、參考代碼
- 不顯示完整 logcat
- 不改動原本草稿與上傳流程，只在失敗出口補提示

## 文件與架構備註

### 保留閱讀順序

若要快速進入專案，建議優先看這些檔案：

- `MainActivity.kt`
- `GutterFormActivity.kt`
- `AddGutterBottomSheet.kt`
- `CameraOverlayFragment.kt`
- `GutterRepository.kt`
- `GutterSessionRepository.kt`

### 這份日誌的用途

原本分散的 API 規格、架構圖、修改紀錄、視覺比較與清單型文件，現在都以這份日誌為唯一入口。後續如果再有新修改，優先直接補在這裡，不要再拆新的紀錄檔。

## 維護提醒

- `README.md` 與 `AGENTS.md` 保留作為專案說明與協作規範
- 其他零散的修改紀錄已整合到本檔
- 如果之後有新的功能調整，請直接更新這份日誌

